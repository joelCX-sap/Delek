<mvc:View
    controllerName="delek.financial.analysis.controller.Chat"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    displayBlock="true">

    <VBox
        id="idChatRoot"
        height="100%"
        alignItems="Stretch"
        class="delekChatRoot">

        <!-- ============================================================
             JOULE HEADER BAR
             ============================================================ -->
        <HBox
            id="idChatHeader"
            alignItems="Center"
            justifyContent="SpaceBetween"
            class="delekChatHeader">
            <HBox alignItems="Center">
                <core:Icon
                    src="sap-icon://ai"
                    size="1.25rem"
                    class="delekChatHeaderIcon sapUiTinyMarginEnd" />
                <VBox>
                    <Title text="Financial AI Assistant" level="H4" class="delekChatHeaderTitle" />
                    <Text text="Powered by SAP AI Core" class="delekChatHeaderSubtitle" />
                </VBox>
            </HBox>
            <Button
                id="idChatClearBtn"
                icon="sap-icon://delete"
                type="Transparent"
                tooltip="Clear conversation"
                press="onClearConversation"
                class="delekChatClearBtn" />
        </HBox>

        <!-- ============================================================
             SCROLLABLE MESSAGE AREA
             ============================================================ -->
        <VBox
            id="idChatMessagesContainer"
            class="delekChatMessagesContainer"
            fitContainer="true">

            <!-- Message list (populated by controller) -->
            <List
                id="idChatMessageList"
                showSeparators="None"
                backgroundDesign="Transparent"
                class="delekChatMessageList" />

            <!-- EMPTY STATE: Joule-style welcome -->
            <VBox
                id="idChatEmptyState"
                visible="true"
                class="delekChatEmptyState"
                alignItems="Center"
                justifyContent="Center">

                <!-- AI Avatar -->
                <VBox class="delekJouleAvatar sapUiSmallMarginBottom" alignItems="Center">
                    <core:Icon
                        src="sap-icon://ai"
                        size="2.5rem"
                        class="delekJouleAvatarIcon" />
                </VBox>

                <Title
                    text="Hello, I'm your Financial AI Assistant"
                    level="H3"
                    textAlign="Center"
                    class="delekChatWelcomeTitle sapUiSmallMarginBottom" />

                <Text
                    text="Ask me anything about G/L accounts, cost centers, suppliers, variances, and financial performance."
                    textAlign="Center"
                    class="delekChatHintText sapUiSmallMarginBottom" />

                <!-- Suggestion chips — horizontal wrapping -->
                <FlexBox
                    wrap="Wrap"
                    justifyContent="Center"
                    class="delekChatSuggestions sapUiSmallMarginTop">
                    <Button
                        text="Show biggest variances"
                        type="Transparent"
                        press="onSuggestionPress"
                        class="delekChatSuggestionBtn" />
                    <Button
                        text="Top cost centers"
                        type="Transparent"
                        press="onSuggestionPress"
                        class="delekChatSuggestionBtn" />
                    <Button
                        text="Largest supplier increase"
                        type="Transparent"
                        press="onSuggestionPress"
                        class="delekChatSuggestionBtn" />
                    <Button
                        text="Why did account 61005002 increase?"
                        type="Transparent"
                        press="onSuggestionPress"
                        class="delekChatSuggestionBtn" />
                    <Button
                        text="Compare FY2025 vs FY2026"
                        type="Transparent"
                        press="onSuggestionPress"
                        class="delekChatSuggestionBtn" />
                    <Button
                        text="What caused repair &amp; maintenance variance?"
                        type="Transparent"
                        press="onSuggestionPress"
                        class="delekChatSuggestionBtn" />
                </FlexBox>

            </VBox>

        </VBox>

        <!-- ============================================================
             ERROR STRIP
             ============================================================ -->
        <MessageStrip
            id="idChatErrorStrip"
            text=""
            type="Error"
            showIcon="true"
            showCloseButton="true"
            visible="false"
            class="delekChatErrorStrip"
            close="onErrorStripClose" />

        <!-- ============================================================
             THINKING INDICATOR (Joule-style)
             ============================================================ -->
        <HBox
            id="idChatBusyRow"
            visible="false"
            alignItems="Center"
            class="delekChatBusyRow">
            <VBox class="delekThinkingBubble">
                <HBox alignItems="Center">
                    <core:Icon src="sap-icon://ai" class="delekThinkingIcon sapUiTinyMarginEnd" />
                    <Text text="AI Assistant" class="delekThinkingLabel sapUiTinyMarginEnd" />
                </HBox>
                <HBox alignItems="Center" class="delekThinkingDots sapUiTinyMarginTop">
                    <VBox class="delekDot delekDot1" />
                    <VBox class="delekDot delekDot2" />
                    <VBox class="delekDot delekDot3" />
                </HBox>
            </VBox>
        </HBox>

        <!-- ============================================================
             STICKY COMPOSER AREA
             ============================================================ -->
        <VBox width="100%" alignItems="Stretch" class="delekChatComposer">

            <!-- Input row: Toolbar always stretches to full width in SAPUI5 -->
            <Toolbar class="delekChatInputRow sapUiNoContentPadding">
                <TextArea
                    id="idChatInput"
                    placeholder="Ask about accounts, cost centers, suppliers, variances..."
                    rows="2"
                    growing="false"
                    width="auto"
                    maxLength="1000"
                    liveChange="onInputLiveChange"
                    class="delekChatTextArea" />
                <Button
                    id="idChatSendBtn"
                    icon="sap-icon://paper-plane"
                    type="Emphasized"
                    enabled="false"
                    tooltip="Send message (Enter)"
                    press="onSendMessage"
                    class="delekChatSendBtn" />
            </Toolbar>

            <!-- Footer row: char count + hint -->
            <HBox
                justifyContent="SpaceBetween"
                alignItems="Center"
                width="100%"
                class="delekChatComposerFooter">
                <Text
                    id="idChatCharCount"
                    text=""
                    class="delekChatCharCount" />
                <Text
                    text="Press Enter to send · Shift+Enter for new line"
                    class="delekChatComposerHint" />
            </HBox>

        </VBox>

    </VBox>

</mvc:View>